# 标准输出
定义输出, 参数东西(文本), 标记内部, 标记自动类型转换。
定义写, 参数东西(文本), 标记内部, 标记自动类型转换。
定义换行, 标记内部。

# 标准输入

# 获取命令行参数
定义输入参数(数组(文本)), 标记内部。
# 读取环境变量
定义输入环境变量(文本), 参数名称(文本), 标记内部。

# 导出
导出输出。
导出写。
导出换行。
导出输入参数。
导出输入环境变量。

# TODO
