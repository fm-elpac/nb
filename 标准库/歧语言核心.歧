# 歧语言核心

# 内置数据类型: 简单类型
定义类型u1, 标记内部。
定义类型二, u1。  # 类型别名
定义类型u8, 标记内部。
定义类型u16, 标记内部。
定义类型u32, 标记内部。
定义类型u64, 标记内部。
定义类型u128, 标记内部。
定义类型i8, 标记内部。
定义类型i16, 标记内部。
定义类型i32, 标记内部。
定义类型i64, 标记内部。
定义类型i128, 标记内部。
定义类型f32, 标记内部。
定义类型f64, 标记内部。
定义类型整数, 标记内部。
定义类型实数, 标记内部。
定义类型数字, 标记内部。
定义类型文本, 标记内部。

# 内置数据类型: 复合类型
定义类型函数(T), 泛型T, 标记内部。
定义类型数组(T), 泛型T, 标记内部。
定义类型集合(T), 泛型T, 标记内部。
定义类型字典(K、V), 泛型K、V, 标记内部。

# 特殊运算符: 赋值
定义+=, 参数左(数字)、右(数字), 标记内部, 标记运算符(100)。
定义-=, 参数左(数字)、右(数字), 标记内部, 标记运算符(100)。
定义*=, 参数左(数字)、右(数字), 标记内部, 标记运算符(100)。
定义/=, 参数左(数字)、右(数字), 标记内部, 标记运算符(100)。
定义%=, 参数左(数字)、右(数字), 标记内部, 标记运算符(100)。
定义&=, 参数左(整数)、右(整数), 标记内部, 标记运算符(100)。
定义|=, 参数左(整数)、右(整数), 标记内部, 标记运算符(100)。
定义^=, 参数左(整数)、右(整数), 标记内部, 标记运算符(100)。
定义<<=, 参数左(整数)、右(整数), 标记内部, 标记运算符(100)。
定义>>=, 参数左(整数)、右(整数), 标记内部, 标记运算符(100)。
定义>>>=, 参数左(整数)、右(整数), 标记内部, 标记运算符(100)。

# 运算符 (函数): 中间位置
定义+(数字), 参数左(数字)、右(数字), 标记内部, 标记运算符(30)。
定义-(数字), 参数左(数字)、右(数字), 标记内部, 标记运算符(30)。
定义*(数字), 参数左(数字)、右(数字), 标记内部, 标记运算符(20)。
定义/(数字), 参数左(数字)、右(数字), 标记内部, 标记运算符(20)。
定义%(数字), 参数左(数字)、右(数字), 标记内部, 标记运算符(20)。
定义&(整数), 参数左(整数)、右(整数), 标记内部, 标记运算符(60)。
定义|(整数), 参数左(整数)、右(整数), 标记内部, 标记运算符(60)。
定义^(整数), 参数左(整数)、右(整数), 标记内部, 标记运算符(60)。
定义<<(整数), 参数左(整数)、右(整数), 标记内部, 标记运算符(20)。
定义>>(整数), 参数左(整数)、右(整数), 标记内部, 标记运算符(20)。
定义>>>(整数), 参数左(整数)、右(整数), 标记内部, 标记运算符(20)。

# 运算符 (函数): 前位置
定义+(数字), 参数自己(数字), 标记内部, 标记前运算符(10)。
定义-(数字), 参数自己(数字), 标记内部, 标记前运算符(10)。
定义~(整数), 参数自己(整数), 标记内部, 标记前运算符(10)。

# 特殊函数: 比较
定义==(二), 参数左(T)、右(T), 泛型T, 标记内部, 标记运算符(50)。
定义!=(二), 参数左(T)、右(T), 泛型T, 标记内部, 标记运算符(50)。
定义>(二), 参数左(数字)、右(数字), 标记内部, 标记运算符(50)。
定义<(二), 参数左(数字)、右(数字), 标记内部, 标记运算符(50)。
定义>=(二), 参数左(数字)、右(数字), 标记内部, 标记运算符(50)。
定义<=(二), 参数左(数字)、右(数字), 标记内部, 标记运算符(50)。

# 类型转换
定义文本(文本), 参数自己(文本), 标记内部。
定义文本(文本), 参数自己(数字), 标记内部。
定义文本(文本), 参数自己(函数), 标记内部。
定义文本(文本), 参数自己(数组), 标记内部。
定义文本(文本), 参数自己(集合), 标记内部。
定义文本(文本), 参数自己(字典), 标记内部。

定义整数(整数), 参数自己(数字), 标记内部。
定义整数(整数), 参数自己(文本), 标记内部。
定义实数(实数), 参数自己(数字), 标记内部。
定义实数(实数), 参数自己(文本), 标记内部。
定义数字(数字), 参数自己(数字), 标记内部。
定义数字(数字), 参数自己(文本), 标记内部。

定义u1(u1), 参数自己(数字), 标记内部。
定义二(u1), 参数自己(数字), 标记内部。
定义u8(u8), 参数自己(数字), 标记内部。
定义u16(u16), 参数自己(数字), 标记内部。
定义u32(u32), 参数自己(数字), 标记内部。
定义u64(u64), 参数自己(数字), 标记内部。
定义u128(u128), 参数自己(数字), 标记内部。
定义i8(i8), 参数自己(数字), 标记内部。
定义i16(i16), 参数自己(数字), 标记内部。
定义i32(i32), 参数自己(数字), 标记内部。
定义i64(i64), 参数自己(数字), 标记内部。
定义i128(i128), 参数自己(数字), 标记内部。
定义f32(f32), 参数自己(数字), 标记内部。
定义f64(f64), 参数自己(数字), 标记内部。

# 导出
导出u1。
导出二。
导出u8。
导出u16。
导出u32。
导出u64。
导出u128。
导出i8。
导出i16。
导出i32。
导出i64。
导出i128。
导出f32。
导出f64。
导出整数。
导出实数。
导出数字。
导出文本。
导出函数。
导出数组。
导出集合。
导出字典。

导出+=。
导出-=。
导出*=。
导出/=。
导出%=。
导出&=。
导出|=。
导出^=。
导出<<=。
导出>>=。
导出>>>=。
导出+。
导出-。
导出*。
导出/。
导出%。
导出&。
导出|。
导出^。
导出<<。
导出>>。
导出>>>。
导出~。
导出==。
导出!=。
导出>。
导出<。
导出>=。
导出<=。

# TODO
